language: php

php:
  - 5.6

before_script:
  - travis setup service -f
  - cp .env.travis .env
  - mysql -e 'create database homestead_test;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit

notifications:
  email:
    recipients:
       - rachellesalvadora@gmail.com
    on_success: always
    on_failure: always